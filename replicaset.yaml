apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicaset-2
spec:
  replicas: 2
  selector:
    matchLabels:
      env: production
  template:
    metadata:
      name: my-app
      labels:
        env: production
    spec:
      containers:
      - name: nginx
        image: nginx
