apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-2
spec:
  replicas: 2
  selector:
    matchLabels:
      env: production
  template:
    metadata:
      name: my-app
      labels:
        env: production
    spec:
      containers:
      - name: nginx
        image: nginx
